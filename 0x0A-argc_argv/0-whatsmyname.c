#include "main.h"
#include <stdio.h>

/**
 * main - prints the program name's, followed by a new line.
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
